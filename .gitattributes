# Pin migration SQL and journal to LF so Windows checkouts don't alter line
# endings — `apps/api/src/db/migrate.ts` SHA-256s these files and compares
# against the hash recorded by drizzle-kit (which always sees LF).
apps/api/drizzle/**/*.sql text eol=lf
apps/api/drizzle/**/*.json text eol=lf
